#include<stdio.h>
#include<string.h>

int main()
{
    char str[100];
    printf("enter the string");
    scanf("%s",str);
int i,j,count;
    int n =strlen(str);


      for(i=0;i<n;i++)
      {
        printf("%c = %d ascii = %d",str[i],i,str[i]);
             printf("\n");
      }
      }

